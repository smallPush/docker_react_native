version: '3.8'

services:
  expo:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - ./src:/app
    environment:
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
      - REACT_NATIVE_PACKAGER_HOSTNAME=localhost
